import { useEffect, useState } from "react"


export default function Footer () {
  
  return (
    <div className={`bottom-0 w-full transition-all duration-500 absolute h-30 bg-gradient-to-b from-gray-700 to-blue-gray-800 h-40`}>
      <h1>I am a footer</h1>
    </div>
  )
}